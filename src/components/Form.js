import React, { Component } from 'react';
import Intro from './Intro';

export default class Form extends Component {
  render() {
    return (
      <div className="form">
        <Intro />
      </div>
    );
  }
}
